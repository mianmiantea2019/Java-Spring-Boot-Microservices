server:
  port: 8081
spring:
  datasource:
    url: 
    username: 
    password: 
    driver-class-name: com.mysql.jdbc.Driver
  application:
    name: userservice # eureka
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos
      discovery:
        cluster-name: SH
mybatis:
  type-aliases-package: com.cast.demo.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
